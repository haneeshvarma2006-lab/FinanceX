node_modules
.next
coverage
db/migrations
pnpm-lock.yaml

# Generated by `pnpm tokens:build`; its formatting is the generator's, and a
# test asserts the file matches byte-for-byte.
src/app/tokens.generated.css

# Written by `next typegen` on every build, in Next's own formatting. Prettier
# reformats it, the next build writes it back, and the file shows as modified
# forever. It is generated; the generator's formatting wins.
next-env.d.ts
